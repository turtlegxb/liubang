# Optional fallback configuration. Command-line arguments, the current process
# environment, and ~/.zshrc values can also supply these settings.
SCHWAB_APP_KEY=
SCHWAB_APP_SECRET=
SCHWAB_TOKEN_PATH=tokens/schwab_token.json

# Optional. Used only when scripts/generate_signals.py --send-discord is passed.
DISCORD_WEBHOOK_URL=
